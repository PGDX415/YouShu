# 有数 — 家庭财务报告 App 开发需求文档

**App 名称**：有数
**Slogan**：有数，家的每一笔，心里有数

## 项目概述
一款极简、快速记账的家庭财务 App，面向日常生活场景。核心目标：**记一笔账不超过3秒**。支持家庭成员共享账本，实时同步。

---

## 技术栈

- **语言/框架**：Swift + SwiftUI（原生开发）
- **目标平台**：iOS 17+、iPadOS 17+（Universal App，一套代码库自适应两种屏幕）
- **数据持久化**：SwiftData（或 Core Data，二选一，优先 SwiftData）
- **同步方案**：CloudKit（通过 iCloud 私有数据库做个人数据同步，共享数据库/CKShare 做家庭多人共享）
- **架构模式**：MVVM
- **最低部署目标**：iOS 17.0
- **Xcode 项目名**：YouShu（或 Youshu），Bundle ID 建议格式：`com.你的域名或名字.youshu`
- **App 显示名称（App Store / 主屏幕图标下方）**：有数

---

## 核心功能范围（第一版 MVP）

### 1. 记账（最高优先级，极致简化）
- 首页默认展示一个醒目的"+"按钮，点击后直接进入记账流程
- 记账流程：**输入金额 → 选择分类图标 → 完成**（2步操作，其余字段全部可选）
- 分类采用图标+颜色，横向/网格排列，一屏展示常用分类（8-10个），不做多级分类菜单
- 可选字段（默认折叠，不强制填写）：备注、账户、日期（默认当天）、所属家庭成员
- 支持"最近使用金额/分类"快速复用

### 2. 预设分类
**支出**：餐饮、交通、购物、居住、娱乐、医疗、教育、其他
**收入**：工资、红包/礼金、其他
- 支持用户自定义新增/编辑分类（图标+颜色可选）

### 3. 首页总览
- 本月收入、支出、结余，三个数字一目了然
- 最近5-10笔交易记录列表（含分类图标、金额、时间）
- 不做复杂仪表盘，保持一屏可见

### 4. 报表
- 月度收支趋势图（折线图，近6个月）
- 分类占比图（饼图，当月支出分类分布）
- 按月/按年切换时间维度
- 点击某个分类可下钻查看该分类下的交易明细

### 5. 家庭多人共享（第一版必备）
- 创建/加入"家庭账本"（通过 iCloud 共享邀请，使用系统原生分享面板发送邀请链接）
- 家庭账本内所有成员的记账实时同步，所有人可见
- 每笔交易标注记录人（显示头像/姓名首字母）
- 首页/报表支持筛选"全部成员"或"仅我"
- 家庭成员管理：查看成员列表、移除成员（仅创建者可操作）

### 6. 账户管理（简化版）
- 支持添加多个账户（现金、储蓄卡、信用卡等），记账时可选择账户，默认"现金"
- 首页可查看各账户余额（简单加总，非银行级对账）

---

## 数据结构建议

```
Transaction（交易记录）
- id
- amount（金额）
- type（收入/支出）
- category（分类，关联Category）
- account（账户，关联Account）
- date（日期时间）
- note（备注，可选）
- createdBy（记录人，关联FamilyMember）
- familyLedgerId（所属家庭账本）

Category（分类）
- id, name, icon, color, type（收入/支出）

Account（账户）
- id, name, icon, balance

FamilyLedger（家庭账本）
- id, name, members[], createdBy, createdAt

FamilyMember（家庭成员）
- id, name, avatarInitial, role（创建者/成员）
```

---

## UI / 交互要求

- **设计语言**：遵循 iOS Human Interface Guidelines，使用系统原生控件（避免自定义过度设计）
- **深色模式**：完整支持
- **iPad 适配**：使用 NavigationSplitView 实现侧边栏+详情的双栏布局（左侧：首页/报表/设置导航；右侧：内容区），而非简单放大iPhone布局
- **动效**：记账完成后有轻量反馈动画（如金额飞入总览、Haptic震动反馈）
- **无强制注册登录**：仅依赖用户已登录的 Apple ID / iCloud 账号，无需额外账号体系
- **无广告、无内购弹窗打扰**（第一版不考虑商业化）

---

## 明确不做（第一版范围外，避免过度设计）

- ❌ 银行卡/微信/支付宝账单自动导入
- ❌ AI 拍照识别小票
- ❌ 预算设定与超支提醒
- ❌ 投资账户跟踪、资产负债表
- ❌ 多币种支持
- ❌ 数据导出（Excel/PDF）
- ❌ Widget / Apple Watch 端（可作为后续迭代）

---

## 验收标准

1. 新用户打开App，10秒内能完成第一笔记账
2. 创建家庭账本并邀请另一台设备加入，双方记账能在5秒内互相同步可见
3. iPhone 和 iPad 上界面均无拉伸变形、留白异常等适配问题
4. 深色模式下所有页面文字/图标对比度正常
5. 断网情况下仍可正常记账，联网后自动同步（CloudKit 离线支持）

---

## 给 Claude Code 的额外说明

请按以下顺序实现：
1. 先搭建 SwiftData 数据模型 + 基础 CRUD
2. 实现记账主流程（不含共享）
3. 实现首页总览 + 报表页面
4. 接入 CloudKit 实现 iCloud 同步
5. 实现家庭账本共享功能（CKShare）
6. iPad 布局适配（NavigationSplitView）
7. 深色模式检查 + 细节打磨

如遇到 CloudKit 共享（CKShare）配置复杂的情况，请优先给出清晰的 Apple Developer 后台配置步骤说明（如 iCloud Container 设置、CloudKit Dashboard schema 配置等），因为这部分需要人工在 Xcode/Developer 后台操作。
